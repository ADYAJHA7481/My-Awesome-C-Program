//Search & find out which is better malloc() or calloc().
/* Memory Allocation----> It is the full form of malloc which means a single dynamic memory block is allocated during runtime.

Contiguous Allocation------> It is the full form of calloc which means multiple memory blocks are allocated to a single variable.

*/